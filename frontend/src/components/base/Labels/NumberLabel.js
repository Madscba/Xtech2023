function NumberLabel({children}) {
    return (
        <span className="flex items-center justify-center p-2 text-sm font-semibold text-teal-500 bg-primary/10 rounded">
            {children}
        </span>
    );
  }
  
export default NumberLabel;
  